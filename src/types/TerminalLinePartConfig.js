/**
 * @typedef {object} TerminalLinePartConfig
 * @property {string} [body] The line part's content.
 * @property {string[]} [headings] Table headings.
 * @property {string[][]} [rows] The rows of the table.
 * @property {import('../renderer/data/TERMINAL_LINE_PART_TYPES.js').TERMINAL_LINE_PART_TYPES} type The type of part to be rendered.
 */
export const TerminalLinePartConfig = {}
