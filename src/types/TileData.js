/**
 * @typedef {object} TileData
 * @property {string} id The ID of the tile within its resourcepack.
 * @property {boolean} isBlocking Whether the tile blocks the robot from entering its cell.
 * @property {boolean} isTraversable Whether the tile is traversable by the robot.
 * @property {string} name The name of the tile within its resourcepack.
 */
export const TileData = {}
